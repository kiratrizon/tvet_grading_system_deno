<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{{ await $slots.title?.() || 'Default Title' }}}</title>
</head>
<body>
  <header>
    {{{ await $slots.header?.() || '<h1>Site Header</h1>' }}}
  </header>

  <main>
    {{{ await $slots.content?.() }}}
  </main>

  <footer>
    {{{ await $slots.footer?.() || '<p>Footer content</p>' }}}
  </footer>
</body>
</html>
